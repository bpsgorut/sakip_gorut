<?php

namespace App\Helpers;

class BreadcrumbsHelper
{
    /**
     * Format breadcrumbs with additional logic if needed
     *
     * @param array $breadcrumbs
     * @return array
     */
    public static function format(array $breadcrumbs): array
    {
        // Tambahkan logika khusus di sini jika diperlukan
        return $breadcrumbs;
    }
}